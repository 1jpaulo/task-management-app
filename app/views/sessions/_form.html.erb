<%= form_with(model: user, url: sessions_path) do |form| %>
  <% if flash.present? %>
    <div style="color: red">
      <ul>
        <% flash.each do |tag, msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :email, class: "block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :password, class: "block" %>
    <%= form.password_field :password %>
  </div>

  <div class="mt-4">
    <%= form.submit "Login" %>
  </div>
<% end %>
