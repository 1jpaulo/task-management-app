<p style="color: green"><%= notice %></p>

<% content_for :title, "Tasks" %>

<h1>Tasks</h1>

<%= link_to "New task", new_task_path %>

<div id="tasks">
  <%# TODO: display as a table with column filtering by priority and due date
    # this filtering may be done server side for faster development
    # if this was to be filtered with js in client-side I would need to pass all data
    # at once and paginate client-side, thus transfering more data. %>
  <%# TODO: check task controller index action %>
  <% @tasks.each do |task| %>
    <%= render task %>
    <p>
      <%= link_to "Show this task", task %>
    </p>
  <% end %>
</div>
